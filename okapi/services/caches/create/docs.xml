<xml>
    <brief>Create a new geocache</brief>
    <issue-id></issue-id>
    <desc>
        <p>This method allows users to create a new geocache.</p>
    </desc>
    <req name='cache_name'>
        <p>Name of the cache. Must not be empty or exceed 256 characters.</p>
    </req>
    <req name='cache_type'>
        <p>Type of the cache. Must be one of: Traditional, Multi, Quiz, Moving, Virtual, Webcam, Other, Event, Own, Podcast.</p>
    </req>
    <req name='latitude'>
        <p>Latitude of the cache location. Must be between -90 and 90.</p>
    </req>
    <req name='longitude'>
        <p>Longitude of the cache location. Must be between -180 and 180.</p>
    </req>
    <opt name='difficulty' default='null'>
        <p>Difficulty rating of the cache. Must be between 1.0 and 5.0.</p>
    </opt>
    <opt name='terrain' default='null'>
        <p>Terrain rating of the cache. Must be between 1.0 and 5.0.</p>
    </opt>
    <opt name='size2' default='small'>
        <p>Size of the cache container. One of: none, nano, micro, small, regular, large, xlarge, other.</p>
    </opt>
    <opt name='short_description' default=''>
        <p>One-line tagline for the cache. Must not exceed 255 characters.</p>
    </opt>
    <opt name='description' default=''>
        <p>Full HTML description of the cache.</p>
    </opt>
    <opt name='hint2' default=''>
        <p>Plain-text hint or spoiler for the cache.</p>
    </opt>
    <opt name='date_hidden' default='today'>
        <p>ISO 8601 date when the cache was/will be hidden. Defaults to today's date.</p>
    </opt>
    <opt name='req_passwd' default='false'>
        <p>Boolean. If true, a password is required to submit "Found it" log entries.</p>
    </opt>
    <opt name='alt_wpts' default=''>
        <p>JSON array of additional waypoints. Each waypoint object should have: name, location (lat|lon format), type, description.
        Waypoint types: parking, trailhead, path, stage, physical-stage, virtual-stage, final, poi, other.</p>
    </opt>
    <common-format-params/>
    <returns>
        <p>A dictionary of the following structure:</p>
        <ul>
            <li>
                <p><b>success</b> - true, if the cache was created successfully.</p>
            </li>
            <li>
                <p><b>message</b> - status message for the user.</p>
            </li>
            <li>
                <p><b>cache_code</b> - the unique code assigned to the newly created cache.</p>
            </li>
            <li>
                <p><b>cache_id</b> - the internal ID of the newly created cache.</p>
            </li>
            <li>
                <p><b>alt_wpts_count</b> - number of additional waypoints created.</p>
            </li>
            <li>
                <p><b>url</b> - direct URL to the cache on the OC website.</p>
            </li>
        </ul>
    </returns>
</xml>