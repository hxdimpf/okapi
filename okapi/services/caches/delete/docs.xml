<xml>
    <brief>Archive/delete a geocache</brief>
    <issue-id></issue-id>
    <desc>
        <p>This method allows the cache owner to archive (delete) a geocache.
        Only the cache owner can delete a cache. When a cache is archived:</p>
        <ul>
            <li>The cache record is archived/deleted</li>
            <li>Associated waypoints are archived/deleted</li>
            <li>All log entries remain in the system as historical records (they are NOT deleted)</li>
            <li>Users can still view logs associated with the archived cache</li>
        </ul>
    </desc>
    <req name='cache_code'>
        <p>Code of the cache to archive/delete. The authenticated user must be the cache owner.</p>
    </req>
    <opt name='reason'>
        <p>Optional reason for the deletion (for audit logging).</p>
    </opt>
    <common-format-params/>
    <returns>
        <p>A dictionary of the following structure:</p>
        <ul>
            <li>
                <p><b>success</b> - true, if the cache was deleted successfully.</p>
            </li>
            <li>
                <p><b>message</b> - status message confirming the deletion and noting that logs remain.</p>
            </li>
        </ul>
    </returns>
</xml>